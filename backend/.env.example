# Required for backend to sign and submit moves
PRIVATE_KEY=0x...

# DRPC RPC URL for Monad testnet (required) - keep private, never expose in frontend
DRPC_RPC_URL=https://lb.drpc.live/monad-testnet/YOUR_API_KEY

# Optional: server port
PORT=3001
